# Python Style Rules

- Keep public functions pure and easy to test.
- Add type hints to every public function.
- Add a concise one-line docstring to every public function.
- Validate inputs at function boundaries.
- Use descriptive names and keep functions small.
- Represent money in float USD and round to two decimal places at boundaries.